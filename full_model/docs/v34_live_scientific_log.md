# V34 live scientific log

All times and source identities below are campaign records, not retroactive
labels for V33 evidence.  The frozen V33 classification remains
`INTEGRATED_I1_VERIFIED_I2_ENERGY_OPEN`.

## 2026-09-16 restart audit

- Canonical worktree: `/Users/sdillon/HPC3/worktrees/asb-drx-full-v34-recovery`.
- Canonical branch: `exp/full-v34-recovery-v1`; clean restart SHA `8b2f77c`.
- GitHub `ls-remote` agreed with `8b2f77c`; a stale local tracking ref was
  refreshed without changing the worktree.
- The configured `uci-hpc3` alias reached `login-i17`.  Slurm contained only
  unrelated job `55950433`, which was not changed.
- Source-frozen local Mura PID 8285 (`e7aa16e`) and ASB manager PID 5163 with
  worker PID 5198 (`fb21dbc`) were live and were not modified.

## Complete common-front energy

Production source checkpoints `958714f`, `b24a4ac`, `e6a8012`, and `b60cb09`
progressively introduced the immutable trial, complete physical-energy
decision, exact zero-event identity, phase-only stationary publication, and
the persistent heat/export/first-law ledger.

The accepted trial evaluates the reconstructed common state at fixed mean
strain.  Its physical owners are local-plus-gradient signed defect storage,
signed junction storage/reaction energy, separately represented boundary
excess, recoverable elastic energy, intrinsic phase barrier and gradient
energy, and thermal internal energy.  Numerical augmented constraints are
reported as zero and cannot decide acceptance or become heat.  Applied-front
work, material-sink export, and prescribed-temperature thermostat export are
separate exchanges.

The finite 32-square manufactured transaction sweeps `1.12e-23 m3`, contains
nonzero signed forest, wall, and junction inventory, and closes line content
to `1.46e-16` relative.  A downhill event is accepted; a high-boundary-energy
uphill event is rejected with every accepted owner and ledger unchanged; the
zero event is bitwise identical.  The accepted first-law residual is below
`1e-29 J`.  This is operator evidence, not spontaneous migration.

An independent Nye assembly differentiates each owner beta and each support
separately.  Identical owner beta fields produce only roundoff interface Nye;
a manufactured beta jump produces the expected signed support-gradient term.
The exact spectral curl and continuum product-rule assembly differ by a
declared representation residual rather than a remainder defined and restored
by subtraction.

## Complete-affinity Mura rule

Source `873a66e` selects families with the complete fixed-strain finite-event
affinity and reevaluates the selected families jointly.  The unreacted part is
a deterministic constrained rate with no pending debt; elapsed physical time
is counted once.  Evidence `28943ca` covers both preserved 64-grid checkpoints,
load, hold, +25 K, released-constraint external energy, timestep refinement,
and a selected 128-grid state.  The operator is qualified; broad B2 and a new
long trajectory remain open.  The frozen `e7aa16e` continuation remains
attributable only to its original rule.

## Canonical regression and production smoke

At source `e6a8012`, `PYTHONPATH=.:src pytest -q tests` completed with
`653 passed in 258.38 s`.  A two-step production combined smoke run from the
same source completed with nonzero slip and beta, an accepted common-front
event, complete-energy first-law residual `5.41e-27 J`, and newly separated
positive/negative/absolute/net sweep plus component-normal displacement.
Its displacement was only `2.09e-19 m`; it therefore remains an I1-scale smoke
test and is not the finite I3 result.

## Active branches

- Phase proposal: the converged unguarded archived n128 replay still creates
  disconnected one-cell sign births despite decreasing complete energy.  A
  topology active set that removes only disconnected births crosses the old
  terminal while retaining attached front motion; symmetry and resolution
  qualification are in progress.
- Thermal causality: the source-frozen 900 K finite-bath control is finishing
  before the five V34 causal continuations are launched, so local capacity is
  not oversubscribed.
- Source-frozen Mura: its terminal status is reconciled below.

## Phase proposal qualification

Source `31bba7d` replaces the sequential clipped explicit Allen--Cahn trial
with a simultaneous simplex-tangent, error-controlled IMEX proposal and an
existing-boundary topology active set.  The active set prevents disconnected
phase births; it does not create a grain or suppress attached translation.
The legacy mode reproduces `UNAUTHORIZED_PHASE_ISLAND`.  At the unchanged
physical parameters, n128/n192 forward displacement differs by only
`7.78e-10` relative, both reverse signs pass, energy decreases, and no
topology projection is used in the smooth finite-translation fixtures.
Classification: `QUALIFIED_EXISTING_BOUNDARY_PHASE_PROPOSAL`; nucleation and
general multiphase topology remain outside its scope.

## Resolved I3 audit and activation-event correction

The first n128/n192 I3 calculation exposed two topology ownership defects:
an algebraic zero level was interpreted as an open contour and an upper-seam
coordinate a few ulps below the periodic box was assigned a second node.
Sources `657bef1` and `3695ecf` add sign-symmetric zero-level handling and
periodic seam canonicalization with explicit regressions.

The initial apparent 27 nm front advance was then rejected as scientific
evidence.  It used one grid cell as the activation volume, grid spacing as the
event jump, a 1 microsecond front allowance, and a 2 ns Mura increment.  The
directive explicitly forbids putting an arbitrary grid-cell free energy into
the event Boltzmann factor.  Source `b20190a` therefore declares one `b^3`
activation volume and one Burgers-vector jump in production, and the final I3
matrix uses a common 2 ns Mura, phase, and front interval.

The corrected existing-bicrystal matrix publishes front-only, Mura-only,
combined, prescribed-temperature, and reversed-contrast controls at n128 and
n192.  The combined velocity is `1.94145e-4 m/s`; component displacement is
`3.8827e-13 m`, only `1.294e-6` of the 0.30 micrometre interface width.  The
n128/n192 displacement difference is `2.10e-6` relative.  Maximum slip is
`1.0409e-8`, the front free-energy change is downhill, signed closure is zero,
and first-law residuals are below `1.6e-26 J`.  Reciprocal effects are
negligible at this horizon and the schema cannot yet transport line moments
through a second Mura/front cycle.

I2 is promoted to `I2_COMPLETE_ENERGY_PRODUCTION_QUALIFIED`.  I3 is classified
`MATCHED_TIME_I3_EXECUTED_PHYSICAL_FRONT_RESPONSE_TOO_SMALL_FOR_I3_AMPLITUDE_GATE`:
shared-state execution is real and grid-convergent, but the quarter-width and
coupling-split gates are not passed.  No spontaneous DRX claim is made.

## Live source-frozen work at final checkpoint preparation

- The historical V32 Mura continuation terminated at step 1451 and strain
  `0.038412781` under its frozen old work-budget rule.  Its failure record is
  preserved; V34's complete-affinity forks use that checkpoint but do not
  relabel the old trajectory.
- The V34 five-case thermal manager remains healthy on immutable source
  `0c45d03` and has reached step 1300 in its first pair.  Its nominal selective
  controls are invalid for causal interpretation because their overrides did
  not enter the authoritative common residual.  Source `06de449` repairs the
  flow/recovery routing, passes 32 focused tests and two-step energy/invariant
  preflights, and runs corrected frozen-flow then frozen-recovery cases from
  the checksum-identical step-100 state under manager PID 32055.  No strict-ASB
  claim is made before valid controls terminate.
- HPC3 was rechecked through `uci-hpc3`; no campaign Slurm jobs were present.

The final integrated suite at implementation/evidence checkpoint `5b1faf5`
completed with `673 passed in 318.82 s` using
`PYTHONPATH=src:. python -m pytest -q tests`.
