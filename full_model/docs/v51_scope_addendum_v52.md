# V51 scope addendum recorded by V52

V51's completed loading/hold pair, one-species climb event measure, positive
line-energy map, and independently audited boundary/surface compatibility are
retained. The following two interpretations are narrowed without rewriting
V51 evidence.

## Optional no-exchange branch

The former area-event test changed kinetic metadata to
`glide_no_exchange` but retained an xy swept surface and the BCC Burgers vector
with nonzero `b_z`. Its integrated plastic-volume increment for the 10 nm
contraction is `-1.1454629341e-24 m3`; zero species parameters did not make the
motion volume preserving. V52 therefore treats that fixture as a negative
mechanism/geometry control. Single- and shared-face publication now require
both `b dot n` and the per-face plastic-volume increment to vanish for the
no-exchange label. Opposite climb faces cannot pass by global cancellation.
The current xy BCC rectangle consequently leaves pure glide unsupported rather
than mislabeling climb. The repaired climb channel is unchanged.

## Temporal-comparison scale

V51's full-versus-two-half fork advanced one macro from the completed loading
endpoint; it was not a refined replay of the full 25.390625 microsecond
history. The physically relevant increment normalizations are:

- plastic-shear increment difference: 0.50685%;
- mean-temperature increment difference: 0.50526%;
- mean temperature-rise endpoint difference: 0.02893%;
- stress-increment difference: 0.27172%.

All remain below the provisional 5% selected-observable threshold. The former
temperature norm divided by approximately 1100 K is retained only as an array
diagnostic, not the physical thermal uncertainty. Density/ordering-field and
whole-history temporal convergence remain unclaimed.
