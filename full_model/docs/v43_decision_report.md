# V43 geometry-capable organization and spatial-repair decision

## Decision

V43 qualifies one nontrivial geometry-changing operation inside the production
mechanics.  Persistent oriented links are the exact boundary of retained swept
plaquettes; the same event derives scalar line, first moment, plastic
distortion, family Nye, complete physical energy, and heat.  This closes the
capability gap that forced V42 to reject reorientation.  The qualified scope is
the represented plaquette/climb channel only.  It is not yet a general
off-lattice junction network or a spontaneous LAGB mechanism.

The spatial investigation produces a decision-grade negative repair result.
It localizes the first strong discrepancy to Mura evolution, excludes initial
sampling and the front transaction as primary causes, and shows that fourfold
time subdivision does not repair the matched n128/n192 field.  The implicated
production seam mixes donor-cell scalar transport with a spectral
velocity-cross-moment/plastic-curl update and declares no nonlinear de-aliasing.
That is a concrete next numerical task, not a reason to change physical
diffusivity, the gradient coefficient, or the acceptance threshold.

## Geometry qualification

The manufactured closed-loop case has a finite accepted swept area and a
nonzero line-length change.  Endpoint balance and the independently evaluated
`Delta alpha + Curl_h Delta beta_p` residual are exactly zero.  Rejection is an
exact rollback with zero heat.  A second event consumes the actual first-event
state, and continuous versus checkpoint continuation is bitwise exact.  A
translated event is symmetry-equivalent, and the same 0.8-micrometer square
has exactly 3.2 micrometers of line at n16 and n32.

The full production enabled/disabled comparison passes the Mura/Nye hard
invariant and changes `beta_p` only on the enabled path.  Geometry-neutral
ordering remains shared by both controls and now owns identical heat in each.
The signed reverse transfer test uses a polarized ordered donor, exhausts it
without negative density, preserves total Nye, and makes zero extent an exact
identity.

Finite-rate response is non-vacuous with zero external event work.  The fast
low-barrier path accepts one full advance and rejects subsequent full extents
as energetically uphill.  Mid/high-barrier hypotheses accept four smaller
increments, with event extents spanning approximately 0.0134 to 0.239; the
positive-entropy hypothesis accepts three.  These are registered generic
response hypotheses, not PF-fitted parameters or material calibration.  No
grain label can be allocated and physical grain count remains one.

## Spatial first difference and attempted repair

The analytic initial states match.  During macro 1 the first Mura half and
front remain common-mode matched.  The second Mura half produces 4.300% common
complex-coefficient difference, only 0.1237% power difference, but 7.314% RMS
amplitude difference.  Thus a phase/location error appears before a large
power-spectrum error.

At interval 2, the retained macro-1 state begins at that 4.300%/7.314% error.
One Mura half increases it to 8.416% common-mode and 18.971% RMS.  The front
changes these values by less than 0.002 percentage points.  The second Mura
half reaches 12.316% and 27.199%.  This locates the first above-threshold
common-mode difference at interval-2 first Mura and the first strong-RMS
difference at macro-1 second Mura.

The four-substep calculation preserves the initial state, loading, kinetics,
physical diffusivity, gradient coefficient, and Fourier normalization.  After
its first Mura half it gives 9.381% common-mode error and 21.273% RMS error,
worse than the one-step control.  After the second half it reaches 13.683%
common-mode, 1.254% power, and 29.235% RMS-amplitude error, versus 12.316%,
1.008%, and 27.199% for one step.  Ordered inventory is bitwise-equivalent at
each grid under the two time integrations: `1.2448791e-5 m/m` on n128 and
`3.6595123e-6 m/m` on n192.  Its large cross-grid error is therefore a spatial
near-extinction issue, not time exposure.  Time truncation is not accepted as
the repair, and the original 62.5-microsecond classification remains
`UNRESOLVED_SPATIAL_SCALE`.

The observed implementation seam is specific: scalar populations use
positivity-preserving first-order donor-cell face transport, while first
moments use a spectral curl of a local nonlinear `v cross kappa` product and
`beta_p`/family Nye use the spectral complex.  V43 does not claim this is the
unique cause.  The next executable discriminator is one conservative,
compatible face flux for scalar and moment transport, followed by the exact
interval-2 pair.  A de-aliased spectral alternative is secondary and must
preserve the same one-flux kinematic identity.

## Front, restart, and V42 closure

The corrected front record has eight rows per state.  The retained hold has
eight evaluated uphill endpoints, zero publications, and eight rate-rejected
no-ops.  The prepared state has eight evaluated downhill endpoints, four
publications, and four rate-rejected no-ops.  Trial endpoint energy is no
longer inferred from a returned zero-state identity, and the four publications
remain independent counterfactual probes rather than a trajectory.

The archived runner now requires explicit initialize/restart mode and validates
path, checksum, schema or declared migration, fields, step, time, strain, RNG,
and source lineage before step one.  Staged archive execution loaded identical
content under an ordinary filename.  Missing path, bad checksum, and bad
lineage all failed with zero new checkpoint and no continuation status.

V42 job `56166842` completed in 18m38s with exit 0.  Archive SHA-256
`2359d0e3c37cc19f082ab8db88405ef97f3f28961671fcf2c065ba38f6af4659`
is verified.  Its matched 5.01% continuation is a hard-valid physical negative:
`NO_QUALIFIED_BOUNDARY`.  The earlier fresh-start job remains quarantined.

The V42 n128/n192 embedded SHA discrepancy is source-equivalent within the
scientific dependency set.  Between `ee4b805` and `17f63ee`, only unused legacy
topology imports were removed; no equation, initialization, parameter, or
numerical update changed.  V43 also freezes source identity at trajectory
start, removing the dynamic-HEAD provenance defect that produced that mismatch.

## Provenance and claims

The configured regression command is `PYTHONPATH=src:. python -m pytest -q
tests`; 755 tests pass in 269.65 seconds.  The requested canonical remote branch
was externally force-moved to older V33 history.  No force push was performed.
All V43 work is preserved on pushed branch
`exp/full-v34-recovery-v43-20260919`, descended from local V42 checkpoint
`1de2383`.

V43 claims a qualified represented geometry event and a bounded physical
response family.  It does not claim a spontaneous persistent LAGB, DRX, strict
ASB, a production three-dimensional junction law, or material calibration.
