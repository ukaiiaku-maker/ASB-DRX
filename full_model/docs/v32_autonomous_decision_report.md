# V32 autonomous scientific decision report

## Decision summary

V32 advanced the production model in three defensible ways, but it does not
qualify the requested full front + Mura + ASB model.

1. The common Mura work-budget failure was repaired by family-resolved
   complementarity and a fixed-strain energy-feasible event limiter. A later
   exact-zero-flux roundoff failure was isolated, quarantined, and repaired as
   an exact identity. The repaired long trajectory has crossed the old failure
   point with every invariant closed and is now physically near saturation.
2. The physical ASB ledger and common-Mura ownership pass local production and
   exact-restart preflight. The 1100 K heterogeneous adiabatic anchor develops
   strong heating but remains broad before its thermal-validity terminal, so it
   is not strict ASB. A preregistered temperature/rate screen is running.
3. The long front campaign closes as a mechanistic negative. Favorable,
   reversed, and label controls complete, but near-equal cases develop resolved
   forbidden islands at 128 squared. No long-front promotion is justified.

The integrated audit also found that the nominal common-Mura/front flag
combination was not an integration: it silently froze phase motion and left
two owners for signed state. Production now fails closed for that combination.

## Automatically repaired failures

### Mura work-budget acceptance

The first rejected event contained one negative-work Burgers family. V32
stalls that family independently, then backtracks the remaining atomic event
against independently recomputed fixed-total-strain elastic release and the
complete defect free-energy change. A zero-feasible-extent endpoint is a
physical stall. No state clipping, post-step projection, or weakened energy
guard is used. The original repair passed 530 tests.

At 3.841278% strain, a separate numerical edge case appeared: event scale zero
was passed through norm reconstruction and manufactured `8.5 m^-2-cells` of
line, with a `1.1874e-8 J m^-3-cells` false defect-energy increment. The old
trajectory is preserved as `HARD_INVALID_NUMERICAL_ZERO_VELOCITY_ROUNDOFF`.
An identically zero signed velocity now returns the exact input objects and a
zero transport/capture/Nye/energy ledger. The repair passed 531 tests and the
resumed trajectory crossed the former terminal with no projection, nonnegative
heat, and zero recorded first-law residual.

### Front topology handling

The 64-square step-135 split contained only a `0.0166`-cell-squared closed
island. V32 added resolution-aware filtering, conservative ordinary split and
merge identity, and event-step topology backtracking. This admits `0.25390625`
of the later step-161 event with line closure `1.21e-19 m` and exact signed
closure. The next step still loses pair identity with no admissible prefix, so
the scientific result remains negative rather than being tuned around.

### Integrated-mode fail-closed contract

A 32-square combined reproducer had 608 MPa front pressure and 20.7 m/s net
front kinetics but zero proposed sweep because common-Mura setup froze KWC
evolution. The sparse front also reconstructed only mobile signed and scalar
forest/wall fields after Mura had established signed forest/wall,
beta/Nye/alignment, and junction ownership. V32 now rejects this configuration
before any mode rewrite. A future adapter must make all of these fields, plus
front reservoirs, work, heat, and reaction extents, one atomic transaction.

## Bounded parameter and structural comparisons

- Front kinetics varied attempt frequency by 0.25 and 4 times, activation
  enthalpy from 0.30 to 0.40 eV, and conservative transfer/storage partition.
  All short candidates were geometry-limited; no speed optimum was inferred.
- ASB Tier 1 varies temperature at 900/1300 K at `3e4 s^-1` and rate at
  `1e4/1e5 s^-1` at 1100 K. Each point has an adiabatic/isothermal pair with
  fixed seed 43 and unchanged strict criteria. Seeds 42 and 44 remain
  conditional on a strict invariant-clean raw candidate.
- Mura compared incomplete global acceptance, family-resolved complementarity,
  complete work accounting, energy-limited backtracking, and physical stall.
  Only the latter combination respects the accepted-event ownership and first
  law.

No conservation tolerance, numerical compatibility penalty, or ASB threshold
was tuned as a physical parameter.

## Robust and negative mechanisms

- Robust locally: common-Mura population/plastic-distortion/Nye ownership;
  exact restart; independently declared physical dissipation channels;
  bidirectional front detailed balance and conservative line transfer;
  exact single-mode behavior.
- Mechanistic negative: current front dynamics do not remain a single physical
  interface under near-equal perturbations at 128 squared. Both signs terminate
  symmetrically in resolved unauthorized islands.
- Under active exposure: repaired Mura organization is strongly event-limited
  near 3.86% strain. A saturation or 5% organization claim awaits the running
  horizon and matched grid.
- Subcritical ASB: the 1100 K heterogeneous adiabatic anchor heats and softens
  strongly but its active width remains broad; thermal validity arrives before
  the fixed strict conjunction is met.

## Integrated-stage decision

- Mura + physical ASB ledger: invariant-qualified and running condition
  exploration.
- Front + physical ASB ledger: not yet qualified as a complete common energy
  transaction.
- Front + Mura: rejected by explicit dual-owner contract.
- Full front + Mura + ASB: not authorized.

## Calculations still running

- Repaired 64-square Mura continuation from source `e7aa16e`, followed
  conditionally by 128 squared. Its small physical event scale makes the
  remaining 64-square horizon approximately 9.5 hours at the latest cadence.
- Three surviving 128-square V31 ASB controls to step 5000 or a declared
  validity terminal.
- Eight-case 128-square ASB Tier-1 screen from source `fb21dbc`, maximum local
  concurrency two. Tier 2 is not authorized yet.

All are checkpointed at physical-progress and wall-clock intervals no larger
than 15 minutes and write to provenance-separated local roots.

## Claims that remain false

- A physical LAGB or DRX grain has not been demonstrated by the repaired Mura
  branch.
- The front scientific gate has not passed.
- Strict ASB has not been demonstrated or grid/seed qualified.
- Front + Mura is not integrated.
- Full front + Mura + ASB is not qualified.
- Coupled DRX/ASB has not been demonstrated.
- No material-class calibration is claimed.

Machine-readable branch decisions, sources, hashes, and live outputs are in
`v32_front_decision.json`, `v32_mura_zero_velocity_roundoff_repair.json`,
`v32_asb_decision.json`, `v32_integrated_decision.json`,
`v32_case_manifest.json`, and `v32_long_run_index.csv`.
