# V15 claim-scope addendum to the frozen V14 record

The raw V14 local and fetched-HPC JSON files are immutable historical evidence.
Their broad `scientific_gate_passed` field is interpreted only within the
claim limit stored in those files. It does not establish mesh-converged SIBM
thresholds or kinetics.

Retained claims:

- `MOVING_FRONT_CONSERVATION_OPERATOR_QUALIFIED = true`
- `CANONICAL_PINNED_CAP_SIGN_DISCRIMINATION_QUALIFIED = true`
- `V14_HPC3_COMPACT_BUNDLE_PASSED = true`

Claims not established:

- `MESH_CONVERGED_PINNED_CRITICAL_PRESSURE = false`
- `CONVERGED_SIBM_INTERFACE_VELOCITY = false`
- `PRODUCTION_POLYCRYSTAL_SIBM_GROWTH = false`
- `FULL_MODEL_SIBM_GROWTH_MECHANISM_SUPPORTED = false`
- `MATERIAL_CALIBRATION = false`

The V14 cases formerly named `production_bulge_128`,
`production_bulge_192`, and `production_unseeded_128` are preserved under
their historical directory names for provenance. Their scientific classes are
respectively `LOADED_CANONICAL_BICRYSTAL_BULGE` and
`LOADED_CANONICAL_BICRYSTAL_UNSEEDED_CONTROL`. They are not deformed-
polycrystal cases.

The close final amplitudes at 128 and 192 are dominated by their common initial
cap. Their signed amplitude increments have opposite signs, so the loaded
canonical short-horizon result is `INCONCLUSIVE_INSUFFICIENT_DISPLACEMENT` and
is not increment- or velocity-converged.
