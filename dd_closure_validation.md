# DD closure validation

Status: blocked at Gate 0 because qualifying raw DD event datasets and required metadata have not been identified.

No closure has been fitted, frozen, or coupled. In particular, neither the legacy domain-count law nor a prescribed `m(rho,T)` is accepted as `dd_collective_closure_v1`.
